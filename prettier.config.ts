import type { Config } from 'prettier';

const config: Config = {
    useTabs: false,
    tabWidth: 4,
    singleQuote: true,
    semi: true,
    trailingComma: 'all',
    bracketSpacing: true,
    endOfLine: 'lf',
};

export default config;
